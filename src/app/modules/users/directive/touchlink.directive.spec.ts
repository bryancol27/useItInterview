import { TouchlinkDirective } from './touchlink.directive';

describe('TouchlinkDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchlinkDirective();
    expect(directive).toBeTruthy();
  });
});
